const Navbar = () =>{
    return (
        <header className="navbar">
        <nav>
            <a href="#home">Home</a>
            <a href="#about">About</a>
            <a href="#profiles">Profiles</a>
        </nav>
    </header>
    );
}

export default Navbar;